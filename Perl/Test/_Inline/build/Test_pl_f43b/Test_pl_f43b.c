/*
 * This file was generated automatically by xsubpp version 1.9508 from the
 * contents of Test_pl_f43b.xs. Do not edit this file, edit Test_pl_f43b.xs instead.
 *
 *	ANY CHANGES MADE HERE WILL BE LOST!
 *
 */

#line 1 "Test_pl_f43b.xs"
#include "EXTERN.h"
#include "perl.h"
#include "XSUB.h"
#include "INLINE.h"

void dump_hash(SV* hash_ref) {
    HV* hash;
    HE* hash_entry;
    int num_keys, i;
    SV* sv_key;
    SV* sv_val;
    
    if (! SvROK(hash_ref))
        croak("hash_ref is not a reference");
    
    hash = (HV*)SvRV(hash_ref);
    num_keys = hv_iterinit(hash);
    for (i = 0; i < num_keys; i++) {
        hash_entry = hv_iternext(hash);
        sv_key = hv_iterkeysv(hash_entry);
        sv_val = hv_iterval(hash, hash_entry);
        printf("%d", SvTYPE(SvRV(sv_val));
        printf("%s => %s\n", SvPV(sv_key, PL_na), SvPV(sv_val, PL_na));
    }
    return;
}


#line 39 "Test_pl_f43b.c"
XS(XS_main_dump_hash); /* prototype to pass -Wmissing-prototypes */
XS(XS_main_dump_hash)
{
    dXSARGS;
    if (items != 1)
	Perl_croak(aTHX_ "Usage: main::dump_hash(hash_ref)");
    SP -= items;
    {
	SV *	hash_ref = ST(0);
#line 38 "Test_pl_f43b.xs"
	I32* temp;
#line 51 "Test_pl_f43b.c"
#line 40 "Test_pl_f43b.xs"
	temp = PL_markstack_ptr++;
	dump_hash(hash_ref);
	if (PL_markstack_ptr != temp) {
          /* truly void, because dXSARGS not invoked */
	  PL_markstack_ptr = temp;
	  XSRETURN_EMPTY; /* return empty stack */
        }
        /* must have used dXSARGS; list context implied */
	return; /* assume stack size is correct */
#line 62 "Test_pl_f43b.c"
	PUTBACK;
	return;
    }
}

#ifdef __cplusplus
extern "C"
#endif
XS(boot_Test_pl_f43b); /* prototype to pass -Wmissing-prototypes */
XS(boot_Test_pl_f43b)
{
    dXSARGS;
    char* file = __FILE__;

    XS_VERSION_BOOTCHECK ;

        newXS("main::dump_hash", XS_main_dump_hash, file);
    XSRETURN_YES;
}

